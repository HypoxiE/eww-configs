(defpoll disks :interval "100ms" :initial "" "scripts/getusb")

(defwindow disks_bar
	:monitor 0
	:windowtype "dock"
	:geometry (geometry :x "0%"
						:y "60px"
						:width "10%"
						:height "0px"
						:anchor "top center")
	:stacking "overlay"
	(eventbox
		:onhoverlost "eww update disks_is_opened=false && eww close disks_bar"
		(box :class "disks-pannel"
			(literal :content disks)
		)
	)
)